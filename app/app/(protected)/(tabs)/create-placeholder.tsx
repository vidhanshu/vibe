// the create page is outside the tabs group but needs to be opened via tab click, so to show the tab this is placeholder page, if we remove this the tab hides, so this is workaround
export default function CreatePlaceholderScreen() {
  return null;
}
